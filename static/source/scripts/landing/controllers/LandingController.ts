namespace Mappino.Landing {
    export class LandingController {

        public static $inject = [
            '$scope'
        ];

        constructor(
            private $scope: ng.IScope) {
            // -
        }
    }
}