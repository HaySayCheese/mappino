/// <reference path='../_all.ts' />


module mappino.map {
    'use strict';

    export class AppController {
        public static $inject = [
            '$scope'
        ];

        constructor(private $scope) {
            // ---------------------------------------------------------------------------------------------------------
        }
    }
}